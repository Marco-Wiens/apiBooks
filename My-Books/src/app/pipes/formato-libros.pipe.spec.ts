import { FormatoLibrosPipe } from './formato-libros.pipe';

describe('FormatoLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
